Following small helpers are available:

killProcess
Asks for each process matching the searchterm whether you want to kill the process.
After executing a normal kill it checks if the process is still running and you can
decide whether you want to kill with -9.

md5Verify
Utility to verify the MD5 checksum of a (downloaded) file.

